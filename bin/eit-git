#!/usr/bin/env ruby

ARGV.replace ['.'] if ARGV.empty?
Dir.chdir(ARGV.first)

files = `git ls-files`.tr('"', '').gsub(%r{/.+}, '').
        split("\n").uniq#.map{ |file| "'#{file}'" }

system('eit', *files)
