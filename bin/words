#!/usr/bin/env ruby
# encoding: utf-8
def words s; puts s.scan(/\p{han}|\p{latin}+/).size; end
if ARGV.empty?
  words($stdin.read)
else
  path = nil
  words(File.read(path)) while path = ARGV.shift
end
