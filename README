Clone this into ~/ and source .config/dev-tool/bash_profile, or:

How to install?

  $ curl https://github.com/godfat/dev-tool/raw/master/bin/dev-tool > /tmp/dev-tool
  $ ruby /tmp/dev-tool install

How to update?

  $ dev-tool update
